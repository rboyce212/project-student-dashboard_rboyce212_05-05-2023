export const CohortDashboard = (data) => {
  const cohortName = "Temp Name";
  const totalStudents = 20;
  return (
    <div className="cohort-dashboard">
      cohortName={cohortName}
      totalStudents={totalStudents}
    </div>
  );
};
